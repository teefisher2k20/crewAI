# Palette's Journal - CrewAI Flow Visualization UX

## 2025-05-15 - Improving Accessibility for Flow Controls
**Learning:** Using `div` for interactive elements like navigation buttons and accordion headers prevents keyboard navigation and doesn't provide semantic information to screen readers.
**Action:** Always use semantic `<button>` elements for interactive controls and provide descriptive `aria-label`s for icon-only buttons.
