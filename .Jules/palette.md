## 2025-03-31 - [Flow Visualization Accessibility Improvements]
**Learning:** Identifying and converting non-semantic interactive elements (like `div` based buttons) to semantic `<button>` elements with `aria-label` attributes significantly improves accessibility for screen readers and keyboard users with minimal code changes.
**Action:** When implementing custom UI controls, always start with semantic elements and ensure they have descriptive labels and clear focus states.
